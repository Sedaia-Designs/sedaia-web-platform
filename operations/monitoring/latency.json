{
  "displayName": "sedaia-api: App Engine p95 latency above two seconds",
  "combiner": "OR",
  "enabled": true,
  "notificationChannels": ["__NOTIFICATION_CHANNEL__"],
  "documentation": {"mimeType": "text/markdown", "content": "Symptom: App Engine p95 request latency exceeded two seconds for ten minutes. Inspect service/version metrics and logs for project sedaia-web-platform-api-508804. First checks: inspect version latency, instance scaling, and downstream calls. Roll back when the active version introduced sustained latency. Owner: __NOTIFICATION_OWNER__."},
  "conditions": [{
    "displayName": "p95 latency above two seconds for ten minutes",
    "conditionThreshold": {
      "filter": "metric.type=\"appengine.googleapis.com/http/server/response_latencies\" AND resource.type=\"gae_app\" AND resource.label.module_id=\"default\"",
      "comparison": "COMPARISON_GT", "thresholdValue": 2000, "duration": "600s",
      "aggregations": [{"alignmentPeriod": "60s", "perSeriesAligner": "ALIGN_PERCENTILE_95", "crossSeriesReducer": "REDUCE_MAX", "groupByFields": ["resource.label.module_id"]}],
      "trigger": {"count": 1}
    }
  }]
}
