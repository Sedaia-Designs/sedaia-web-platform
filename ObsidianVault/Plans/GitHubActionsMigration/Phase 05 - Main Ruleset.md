# Phase 05 - Main Ruleset

**Status: Pending hosted enforcement evidence.**

Protect `main` against deletion, force pushes, unreviewed merges, unresolved
conversations, and failing or stale required checks. Validate enforcement with
a disposable pull request and record any narrowly scoped bypass.

Exit criterion: normal maintainers cannot place unreviewed or failing changes
on `main`.
